# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: VisDrone
  - override /model: yolo11-P2-FASFFHead #

model: #
  name: yolo11-P2-FASFFHead
  scale: s
  yaml_file: ${model.name}.yaml
  base_model: yolo11

logger:
  tags: ["FASFFHead"]
  is_basic: True

notes: "更换检测头, 188"
