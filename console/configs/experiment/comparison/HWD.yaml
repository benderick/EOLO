# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: VisDrone
  - override /model: comparison/HWD #

model: #
  name: HWD
  scale: x
  yaml_file: ${model.name}.yaml
  base_model: yolo11

logger:
  tags: ["HWD"]
  is_basic: True

notes: ""
