# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: VisDrone
  - override /model: comparison/RFD #

model: #
  name: RFD
  scale: x
  yaml_file: ${model.name}.yaml
  base_model: yolo11

logger:
  tags: ["RFD"]
  is_basic: True

notes: ""
